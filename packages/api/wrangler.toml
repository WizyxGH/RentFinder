# Worker Cloudflare (§28). AUCUN SECRET ICI — ce fichier est versionné.
# Les valeurs sensibles passent par `wrangler secret put` :
# TURSO_DATABASE_URL, TURSO_AUTH_TOKEN, API_ACCESS_TOKEN.

name = "rentfinder-api"
main = "src/index.ts"
compatibility_date = "2026-08-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Votre URL GitHub Pages — pas un secret.
API_ALLOWED_ORIGIN = "https://example-user.github.io"

[observability]
enabled = true
